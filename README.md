# BalanceTweaksForMe

A simple plugin that nerfs Shovel, Knife, Shotgun and Ammo

## Config

CreateNetworkPrefab - Prevent the client from joining if they don't install this plugin, default is false, since all these changes are all nerfs
